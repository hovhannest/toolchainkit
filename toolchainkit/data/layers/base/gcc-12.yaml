type: base
name: gcc-12
compiler: gcc
version: 12.3.0
description: "GNU GCC 12.3.0 - Stable with C++20 support"

flags:
  common:
    - "-fdiagnostics-color=always"
    - "-pthread"
  compile:
    - "-Wall"
    - "-Wextra"
  link:
    - "-fuse-ld=gold"

cmake_variables:
  CMAKE_C_COMPILER: "{{toolchain_root}}/bin/gcc"
  CMAKE_CXX_COMPILER: "{{toolchain_root}}/bin/g++"
  CMAKE_AR: "{{toolchain_root}}/bin/gcc-ar"
  CMAKE_RANLIB: "{{toolchain_root}}/bin/gcc-ranlib"
