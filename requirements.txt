# ToolchainKit Requirements
# Python 3.9+

# Core dependencies
requests>=2.28.0  # HTTP library for downloads
filelock>=3.12.0  # Cross-platform file locking for registry
PyYAML>=6.0  # YAML parsing for configuration files
Jinja2>=3.0.0  # Template engine for bootstrap scripts

# Testing
pytest>=7.0.0
pytest-mock>=3.10.0  # Mocking support for tests
responses>=0.22.0  # Mock HTTP responses for testing
pytest-cov>=5.0.0  # Code coverage reporting
pytest-xdist>=3.0.0  # Distributed testing
